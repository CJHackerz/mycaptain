= simple_form_for(@post) do |f|
	= f.error_notification

	.form-inputs
		= f.association :workshop
		= f.input :title
		= f.input :content
		%h3 YouTube Videos
		#youtube
			= f.simple_fields_for :embedurls do |embedurl|
				= render 'embedurl_fields', f: task
			.links
				= link_to_add_association 'add youtube url', f, :embedurls

	.form-actions
		= f.button :submit
